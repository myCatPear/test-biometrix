import React, { FC } from 'react';

import { FilmsList } from 'features';

import './App.css';

const App: FC = () => {
  return (
    <div className="App">
      <FilmsList />
    </div>
  );
};

export default App;
